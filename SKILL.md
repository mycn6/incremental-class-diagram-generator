---
name: incremental-class-diagram-generator
description: "根据 Git 提交、分支或工作区代码变更建立可核对的结构化事实清单（JSON），并在独立审查报告中分析逻辑错误、校验或测试缺口、代码结构、设计质量和设计模式；需要时可从清单导出可编辑、分区化的 draw.io 增量类图。用于整体方案设计前的代码分析、AI 生成代码验收、合并前评审、重构分析或为 aibid-approach-diagrams 准备可追溯事实；不用于直接绘制通用业务流程图。"
---

# 增量代码事实清单与方案前置分析

先从真实代码变更建立可追溯的结构事实，再输出供整体方案图使用的风险、约束、证据和待确认项。默认交付结构化事实清单与独立审查报告；代码问题和设计建议进入独立审查报告，不叠加到事实或类图。类图是按需从清单导出的可选产物，不在关键路径上。默认中文。

## 确定范围与证据

- 先读取目标仓库的 `AGENTS.md`。直接采用用户指定的基准与目标提交、分支或标签；未指定时检查工作区和近期提交，选择范围并在交付中说明。
- 用 `git diff --name-status`、`git diff`、`rg` 和源码确认变更文件、入口、类型和关系。函数名、文件名、注释或启发式扫描不能单独证明关系。
- 新增、修改、删除类型是主体。执行 Skill 的 Agent 根据源码自动筛选解释直接关键关系所必需的一跳未变类型，不暂停询问用户；接口、父类、核心领域对象和边界类型优先，标准库、框架、工具、异常、普通 DTO 与间接类型默认排除。未变类型直接显示名称，不加 `[未变]` 前缀。删除项以基准版本为依据。
- 区分静态代码、测试和实际运行证据。证据不足的关系标记“待确认”，不要把扫描器能力不足写成系统难点。
- 完整审查先建立范围清单：模块、Git 范围、文件、类型、行数和排除项；范围不完整时不给完整审查结论。

## 建立事实清单

1. 阅读 [事实清单与关系证据契约](references/code-facts-inventory.md)。
2. 扫描并写出清单：

   ```bash
   python <skill目录>/scripts/diagram_gen.py --repo <仓库> --base <基准> --head <目标>
   ```

   未提交改动使用 `--base HEAD --head WORKTREE`；已有文件列表时使用 `--files <文件...>`。默认写到 `docs/code-review/<范围标识>-facts.json`，用 `--facts <路径>` 可改。脚本只保守提取声明、Python 成员与显式继承／实现，且不生成代码问题。目标不在本页的关系（框架与标准库基类）记为 `to: null` 并静默跳过；短名被本页多个类共用的关系同样不解析，并写进 `warnings` 与 stderr，据此回源码确认该连哪个类。

   同一范围重扫按 `origin` 合并：`scan` 条目被新结果替换，`agent` 条目原样保留。目标文件已存在且范围不同时会拒绝写入，需要 `--force` 才覆盖——那会丢弃复核成果，只在你确实想从头重来时才用。

3. 回到源码复核，**编辑清单**：补全有证据的关联、聚合、组合和依赖，把不解析的关系改成确切的类 `id`，补上扫描器没提取到的成员。必要时补充最小的一跳未变类型，`change` 写 `unchanged`、`origin` 写 `agent`，不在可见名称中添加状态前缀。证据不足的关系不写进清单，转入审查报告的待验证事项。每条补充都要有 `source` 与非空 `evidence`。

   清单是 agent 可编辑的产物，必须是严格 UTF-8，中文证据串一经写坏不会自动恢复。

4. 校验清单：

   ```bash
   python <skill目录>/scripts/facts_io.py --check <清单.json>
   ```

   它只对结构性问题硬失败（端点指向不存在的 `id`、`evidence` 为空、`source` 逃出仓库等）；证据串里没出现目标短名这类只告警。

5. 需要图时导出。**导出物是清单的纯函数**：节点尺寸在导出时按当时的成员重新推导，样式器与布局器由生成器内部调用，不需要单独再跑。导出前阅读 [类图导出与 XML 契约](references/drawing-conventions.md)。

   ```bash
   python <skill目录>/scripts/diagram_gen.py --from-facts <清单.json> --output <输出.drawio>
   ```

   类型语义由 `kind` 驱动并自动表达：所有分类器保留矩形外框；接口显示 `«interface»`，抽象类显示 `«abstract»` 且标题为斜体，枚举显示 `«enumeration»` 与常量分区，结构体显示 `«struct»`。属性、操作和枚举常量使用独立子图元，不用分隔线字符模拟。每页图例实际展示这些分区类框、关系箭头和 Git 状态色块，不使用纯文字图例。需要视觉起点时可复制 [导出示例](assets/class-diagram-example.drawio) 对照契约。

6. 校验导出物：

   ```bash
   python <skill目录>/scripts/validate_drawio.py <输出.drawio>
   python <skill目录>/scripts/layout_drawio.py --check <输出.drawio>
   ```

   `validate_drawio.py` 是硬关口：文字装不下、编码丢失、契约缺失都必须改清单后重新导出。`layout_drawio.py --check` 是尽力而为的几何检查——布局器已内部跑过优化，残余的 `edge-crossing` 是它无法在当前位置下分离两段共享同一通道的折线（根因见 `AGENTS.md` 的「已知限制：外围通道的竖折线不分配 x 车道」），**不是可以靠改 XML 消掉的问题**：手工挪拐点会在下次导出时被覆盖。不要手改导出物的几何，也不要为了让它通过而删关系；把残余冲突如实记进报告的「事实清单与类图」项，让复核者知道这份导出在密集汇入处存在折线重叠。几何检查只基于 XML 坐标，不能替代真实字体、箭头、标签和页面尺度的渲染检查。

7. 默认保存到 `docs/code-review/`，与报告同目录；只要图不要报告时按仓库约定放 `docs/diagrams/`。已有仓库约定优先，修改现有图时保留无关内容。

**导出物是派生物：要改内容就改清单再重新导出，不要直接改 XML。** 直接改的会在下次导出时被覆盖。

## 执行质量审查

阅读 [代码质量审查约定](references/code-quality-review.md)，将代码问题和设计建议放入独立报告，不写进清单或 draw.io：

1. 先核验事实清单的覆盖范围和关系，再深入逻辑正确性、校验与测试缺口、类内设计、类间关系、重复与共性提取。
2. 每个结论使用完整中文类别，写明目标类、文件和行号或可定位符号（也可用清单的 `classes[].id` 或某条关系的 `evidence`），以及证据、影响、建议和验证方式。仅根据明确变化信号建议设计模式，并说明收益、代价和不采用风险。
3. 涉及大模型调用时，审查调用封装、代码层输入校验、结构化输出解析、异常处理和降级；不涉及时标记“不适用”。
4. 按参考中的量表和模板输出。低于 80 分必须给出可验收整改项；证据不足时说明限制，不伪造精确结论。

## 输出方案图交接信息

始终在交付摘要中提供可供 `aibid-approach-diagrams` 使用的“方案图输入摘要”；用户明确准备继续生成整体方案图时，将同样内容写入审查报告或仓库约定的交接文档。包括：

- 功能与 Git 范围、核心类职责和关键关系；
- 公共接口、数据、权限、事务、兼容性和模型边界等事实与约束；
- 已确认问题、结构观察、模式识别与机会、优化建议、测试／运行证据和待确认项。

不要在本 Skill 中判断整体方案的重点或难点，也不要提前绘制业务流程。下游 Skill 应重新把类级事实组织为业务动作、代码节点和模型节点，而不是逐个复制类图节点。

## 校验与交付

- 运行清单与导出物检查：

  ```bash
  python <skill目录>/scripts/facts_io.py --check <清单.json>
  python <skill目录>/scripts/validate_drawio.py <输出.drawio>        # 仅在有导出物时
  python <skill目录>/scripts/layout_drawio.py --check <输出.drawio>  # 仅在有导出物时
  ```

  `facts_io.py` 检查清单的结构：关系端点能否解析、`evidence` 是否为空、`source` 是否是仓库内路径。`validate_drawio.py` 检查文件是严格 UTF-8、含中文的单元格声明了 CJK 字体、文本里没有 `?` 形式的编码丢失痕迹，以及每个标签按实测字宽是否装得进自己的节点和分区。文字装不下是硬失败，报错会给出需要的像素数。

- 有导出物时，在 draw.io 或兼容渲染器中逐页检查分区、文字、节点与标签遮挡、箭头方向、线—线交叉、线—节点交叉和路径重合；有问题就回到清单重新导出并复看。无法渲染时明确“已完成结构和 XML 几何检查，尚未完成实际渲染检查”。

- 类图模式交付清单文件链接、Git 范围、主要结构变化、方案图输入摘要、待确认关系和验证范围。

- 完整审查再增加范围清单、评分、问题、模式／共性建议、整改项和复审条件。

- 清单校验只能证明结构，导出物校验只能证明 XML 契约与文字装得下，几何检查只能发现基于坐标的部分冲突；源码真实性、问题结论、业务含义和实际渲染布局必须分别复核。清单里的成员是完整的，导出图受分区行预算约束会丢弃成员并声明 `… 另有 N 项`——两者不一致时以清单为准。

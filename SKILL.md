---
name: incremental-class-diagram-generator
description: "根据 Git 提交、分支或工作区代码变更创建可编辑、分区化的 draw.io 增量类图，并在独立审查报告中分析逻辑错误、校验或测试缺口、代码结构、设计质量和设计模式。用于整体方案设计前的代码分析、AI 生成代码验收、合并前评审、重构分析或为 aibid-approach-diagrams 准备可追溯事实；不用于直接绘制通用业务流程图。"
---

# 增量类图与方案前置分析

先从真实代码变更建立可追溯的结构事实，再输出供整体方案图使用的风险、约束、证据和待确认项。默认交付原生、可编辑、具有真实 UML 分区的 `.drawio`；代码问题和设计建议进入独立审查报告，不叠加到类图。默认中文。

## 确定范围与证据

- 先读取目标仓库的 `AGENTS.md`。直接采用用户指定的基准与目标提交、分支或标签；未指定时检查工作区和近期提交，选择范围并在交付中说明。
- 用 `git diff --name-status`、`git diff`、`rg` 和源码确认变更文件、入口、类型和关系。函数名、文件名、注释或启发式扫描不能单独证明关系。
- 新增、修改、删除类型是主体。执行 Skill 的 Agent 根据源码自动筛选解释直接关键关系所必需的一跳未变类型，不暂停询问用户；接口、父类、核心领域对象和边界类型优先，标准库、框架、工具、异常、普通 DTO 与间接类型默认排除。未变类型直接显示名称，不加 `[未变]` 前缀。删除项以基准版本为依据。
- 区分静态代码、测试和实际运行证据。证据不足的关系标记“待确认”，不要把扫描器能力不足写成系统难点。
- 完整审查先建立范围清单：模块、Git 范围、文件、类型、行数和排除项；范围不完整时不给完整审查结论。

## 生成并复核类图

1. 阅读 [类图绘制与 XML 契约](references/drawing-conventions.md)。需要视觉起点时复制 [可编辑示例](assets/class-diagram-example.drawio)，并以真实代码替换示例内容。
2. 生成语义化初稿：

   ```bash
   python <skill目录>/scripts/diagram_gen.py --repo <仓库> --base <基准> --head <目标> --output <输出.drawio>
   ```

   未提交改动使用 `--base HEAD --head WORKTREE`；已有文件列表时使用 `--files <文件...>`。脚本只保守提取声明、Python 成员与显式继承／实现，默认不自动补未变类型，也不生成代码问题。目标不在本页的关系（框架与标准库基类）静默跳过；短名被本页多个类共用的关系不连线并 WARN 到 stderr，据此回源码确认该连哪个类。
3. 回到源码复核成员与关系，补全有证据的关联、聚合、组合和依赖。必要时自动补充最小的一跳未变类型，并设置 `change=unchanged`；不在可见名称中添加状态前缀。证据不足的关系不连线，转入审查报告的待验证事项。补全和修改 XML 只能通过文件工具或本 Skill 的脚本完成，不要用 shell 重定向、内联脚本或其它不保证 UTF-8 的通道生成或改写 `.drawio`，中文一旦丢成 `?` 不会自动恢复。补完成员后节点尺寸会陈旧，必须重跑 `validate_drawio.py`：它会报出该分区需要多少像素，据此同步改该分区的 `height` 与外层类节点的 `height`。
4. 使用真实可编辑 UML 分区表达类型：所有分类器保留矩形外框；接口显示 `«interface»`，抽象类显示 `«abstract»` 且标题为斜体，枚举显示 `«enumeration»` 与常量分区，结构体显示 `«struct»`。属性、操作和枚举常量使用独立子图元，不用分隔线字符模拟。每页图例实际展示这些分区类框、关系箭头和 Git 状态色块，不使用纯文字图例。
5. 运行机械样式器；它只消费显式元数据，不推断语义或布局：

   ```bash
   python <skill目录>/scripts/style_drawio.py <输出.drawio>
   ```

6. 运行独立几何优化器，按关系层级排列类节点、分配连接端口、写入显式直角拐点，并迭代减少线—节点、线—线交叉和路径重合：

   ```bash
   python <skill目录>/scripts/layout_drawio.py <输出.drawio>
   python <skill目录>/scripts/layout_drawio.py --check <输出.drawio>
   ```

   优化器仍报告冲突时，优先扩大间距、调整外围通道或拆页，不隐藏结果。几何检查只基于 XML 坐标，不能替代真实字体、箭头、标签和页面尺度的渲染检查。
7. 默认保存到 `docs/diagrams/<范围>-incremental-class-diagram.drawio`；完整审查的图和报告默认保存到 `docs/code-review/`。已有仓库约定优先，修改现有图时保留无关内容。

## 执行质量审查

阅读 [代码质量审查约定](references/code-quality-review.md)，将代码问题和设计建议放入独立报告而非 draw.io：

1. 先核验类图覆盖范围和关系，再深入逻辑正确性、校验与测试缺口、类内设计、类间关系、重复与共性提取。
2. 每个结论使用完整中文类别，写明目标类、文件和行号或可定位符号，以及证据、影响、建议和验证方式。仅根据明确变化信号建议设计模式，并说明收益、代价和不采用风险。
3. 涉及大模型调用时，审查调用封装、代码层输入校验、结构化输出解析、异常处理和降级；不涉及时标记“不适用”。
4. 按参考中的量表和模板输出。低于 80 分必须给出可验收整改项；证据不足时说明限制，不伪造精确结论。

## 输出方案图交接信息

始终在交付摘要中提供可供 `aibid-approach-diagrams` 使用的“方案图输入摘要”；用户明确准备继续生成整体方案图时，将同样内容写入审查报告或仓库约定的交接文档。包括：

- 功能与 Git 范围、核心类职责和关键关系；
- 公共接口、数据、权限、事务、兼容性和模型边界等事实与约束；
- 已确认问题、结构观察、模式识别与机会、优化建议、测试／运行证据和待确认项。

不要在本 Skill 中判断整体方案的重点或难点，也不要提前绘制业务流程。下游 Skill 应重新把类级事实组织为业务动作、代码节点和模型节点，而不是逐个复制类图节点。

## 校验与交付

- 运行结构与 XML 几何检查：

  ```bash
  python <skill目录>/scripts/validate_drawio.py <输出.drawio>
  python <skill目录>/scripts/layout_drawio.py --check <输出.drawio>
  ```

  `validate_drawio.py` 同时检查文件是严格 UTF-8、含中文的单元格声明了 CJK 字体、文本里没有 `?` 形式的编码丢失痕迹，以及每个标签按实测字宽是否装得进自己的节点和分区。文字装不下是硬失败，报错会给出需要的像素数。

- 在 draw.io 或兼容渲染器中逐页检查分区、文字、节点与标签遮挡、箭头方向、线—线交叉、线—节点交叉和路径重合；有问题就调整 XML 后重新执行几何检查并复看。无法渲染时明确“已完成结构和 XML 几何检查，尚未完成实际渲染检查”。
- 类图模式交付文件链接、Git 范围、主要结构变化、方案图输入摘要、待确认关系和验证范围。
- 完整审查再增加范围清单、评分、问题、模式／共性建议、整改项和复审条件。
- 结构校验只能证明 XML 契约，几何检查只能发现基于坐标的部分冲突；源码真实性、问题结论、业务含义和实际渲染布局必须分别复核。

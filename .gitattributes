# 本机 core.autocrlf=true 会在检出时把文本文件改写成 CRLF。下面两条 `-text` 只保证
# 「检出不做转换」：文件进出仓库的字节与写下时一致。
#
# 注意 `-text` 只覆盖 checkout 一侧，它管不了脚本写出什么行尾。行尾由写出方自己保证：
# .drawio 的三个序列化点（diagram_gen.write_document、layout_drawio、style_drawio）
# 都以二进制句柄写出（见 AGENTS.md「工程纪律」），因此恒为 LF；`-text` 让这份 LF
# 原样入库、原样检出，不被 core.autocrlf 改写成 CRLF 而破坏字节级契约。
*.drawio -text

# 事实清单同样是 agent 可编辑的产物，必须是严格 UTF-8（中文证据串在其中）。
# facts_io.dump_facts 显式以 newline="\n" 写出，任何行尾转换都会让每次重扫产生一份
# 整文件 diff，把真正改动的那几行淹掉。
*.json -text
